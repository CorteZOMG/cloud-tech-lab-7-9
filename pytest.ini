[pytest]
env_override_existing_values = 1

env_files =
    .test.env

pythonpath = .

asyncio_mode = auto

norecursedirs = env venv docker

addopts =
    --cov=src
    --cov-report=term-missing
    --cov-report=html
